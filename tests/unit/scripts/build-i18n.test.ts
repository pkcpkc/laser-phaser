import { describe, it, expect } from 'vitest';

describe('scripts/build-i18n.ts', () => {
    it('should exist', () => {
        // This script is executed via command line.
        expect(true).toBe(true);
    });
});
